# Public-IP race trial

Record date, host/player networks, ISP/CGNAT status, router model, Windows version, runtime build, UDP port, automatic mapping result, external reachability result, and manual forwarding steps.

Run at least two players for three charts with 100 ms induced RTT, jitter, and loss. Attach in-game diagnostics JSON plus Connect, verification, all-ready, gameplay HUD, and Results screenshots. Confirm the header link state and lifecycle remain legible and the highlighted next action follows each transition. Pass when password failures are rate-limited, start skew is below 50 ms, ranking p95 is below 250 ms, gameplay telemetry is below 15 KiB/s/player, and reconnect journals finish within the 30-second grace. Record UPnP, NAT-PMP, manual, VPN, or CGNAT fallback explicitly.
